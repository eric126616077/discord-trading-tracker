# Discord 私人頻道內容提取器 - 使用者帳號版本快速指南

## ⚠️ 風險警告 ⚠️

使用個人 Discord 帳號 Token 可能：
- 違反 Discord 服務條款 (ToS)
- 導致帳號被暫時或永久封鎖
- 失去帳號所有權

**建議**：僅用於短期測試，長期使用請創建 Discord Bot。

---

## 步驟 1：獲取你的 Discord Token

### 方法一：使用 Discord 網頁版
1. 打開瀏覽器，訪問 https://discord.com/login
2. 按 `F12` 打開開發者工具
3. 切換到 **Network** (網路) 標籤
4. 登入 Discord
5. 在篩選框輸入 `messages`
6. 點擊任何一個請求
7. 在 **Headers** (標頭) 中找到 `authorization` 欄位
8. 複製完整的 Token 值

### 方法二：使用 Discord 桌面版
1. 登入 Discord 桌面版
2. 按 `Ctrl+Shift+I` 打開開發者工具
3. 切換到 **Network** 標籤
4. 發送一條訊息
5. 找到請求，查看 `authorization` 標頭
6. 複製 Token

### Token 範例格式
```
YOUR_TOKEN_looks_like: NzM2NjMwNzE0NzE3Mzk3NzI3.G7BbBb.bbbbbbbbbbbbbbbbbbbbbbbb
```

---

## 步驟 2：獲取頻道 ID

1. 在 Discord 設定中開啟「開發者模式」
   - 設定 > 進階 > 開啟「開發者模式」
2. 右鍵點擊目標私人頻道
3. 選擇「複製 ID」

---

## 步驟 3：配置 Token

編輯 `config/settings.py`：

```python
# Discord 用戶 Token
USER_TOKEN = "你的_Token_貼上這裡"

# 要監控的頻道 ID 列表
CHANNEL_IDS = [
    123456789012345678,  # 貼上你的頻道 ID
]
```

---

## 步驟 4：安裝依賴

```bash
pip install -r requirements.txt
```

---

## 步驟 5：執行

```bash
# 使用用戶版本
python user_main.py
```

成功後會顯示：
```
✓ 已登入: 你的用戶名稱
✓ 監控頻道: [123456789012345678]
```

---

## 步驟 6：訪問網頁介面

打開瀏覽器訪問：`http://localhost:5000`

---

## 🌍 部署到網際網路（讓朋友訪問）

### 方案 A：Cloudflare Tunnel（最簡單，推薦）

1. **下載 cloudflared**：
   - 下載連結：https://github.com/cloudflare/cloudflared/releases
   - Windows 下載 `cloudflared-windows-amd64.exe`

2. **執行隧道**：
```powershell
# 將下載的檔案改名為 cloudflared.exe
.\cloudflared.exe tunnel --url http://localhost:5000
```

3. **訪問方式**：
   - 執行後會顯示：`https://xxx.trycloudflare.com`
   - 將這個網址分享給朋友即可

---

### 方案 B：ngrok

```powershell
# 安裝 ngrok（需註冊帳號）
ngrok http 5000
```

---

### 方案 C：Render.com 雲端部署

1. **上傳到 GitHub**：
   - 創建 GitHub 倉庫
   - 上傳 `discord-extractor` 資料夾

2. **部署到 Render**：
   - 訪問 https://render.com
   - 創建新的 Web Service
   - 連接 GitHub 倉庫
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `gunicorn app:app --bind 0.0.0.0:$PORT`

3. **環境變數**：
   - 在 Render 設定中新增環境變數

---

## 常見問題

### Q: Token 錯誤 401
A: Token 可能已過期，請重新獲取

### Q: 無法獲取私人頻道訊息
A: 確認頻道 ID 正確，且頻道存在

### Q: 程式崩潰
A: 檢查網路連線，嘗試重新獲取 Token

---

## 停止程式

按 `Ctrl+C` 停止監控

---

## 重要提醒

1. **Token 不要分享給任何人**
2. **不要將 Token 上傳到 GitHub**
3. **定期更換 Token**
4. **考慮使用 Bot 版本更安全**
